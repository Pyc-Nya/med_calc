{
  "compilerOptions": {
    "target": "es2020",         /* Целевая версия ECMAScript для скомпилированного JavaScript. es2020 для современных возможностей. */
    "module": "commonjs",       /* Система модулей для сгенерированного JavaScript. Node.js традиционно использует CommonJS. */
    "outDir": "./dist",         /* Каталог, в который будет выводиться скомпилированный JavaScript. */
    "rootDir": "./src",         /* Корневой каталог для исходных файлов TypeScript. */
    "strict": true,             /* Включает все строгие проверки типов. Рекомендуется для лучшего качества кода. */
    "esModuleInterop": true,    /* Позволяет использовать синтаксис импорта по умолчанию (import x from 'y') для CommonJS модулей. */
    "skipLibCheck": true,       /* Пропускает проверку типов файлов декларации (d.ts) из node_modules. Полезно для ускорения компиляции. */
    "forceConsistentCasingInFileNames": true, /* Обеспечивает, что ссылки на один и тот же файл всегда используют одинаковый регистр символов. */
    "resolveJsonModule": true   /* Позволяет импортировать .json файлы как модули TypeScript. Может пригодиться, если решите напрямую импортировать данные. */
  },
  "include": [
    "src/**/*.ts"               /* Указывает TypeScript компилировать все .ts файлы в папке src и ее подпапках. */
  ],
  "exclude": [
    "node_modules",             /* Исключает папку node_modules из компиляции. */
    "**/*.spec.ts"              /* Исключает тестовые файлы (если вы решите их добавить). */
  ]
}